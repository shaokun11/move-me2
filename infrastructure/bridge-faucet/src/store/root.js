/**
 * @type {import('vuex').Module}
 */
export const root = {
  state: {},
  getters: {
    // account:state=>state.user.account,
    // network:state=>state.user.network,
  },
  mutations: {},
  actions: {},
}

export default root
